#! /usr/bin/bash

# writing to a file.txt

echo "This is a new line to be written." >> toWriteTo.txt

# using > operator will overwrite file if already exists
# while using >> operator will append it 
